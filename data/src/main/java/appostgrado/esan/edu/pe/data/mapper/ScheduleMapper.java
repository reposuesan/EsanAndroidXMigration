package appostgrado.esan.edu.pe.data.mapper;

public class ScheduleMapper {
    /* implements Mapper<User, UserEntity> {

    @Override
    public User transform(UserEntity vEntity) {
        User user = new User();
        user.setId(vEntity.getId());
        user.setName(vEntity.getName());
        user.setToken(vEntity.getToken());
        user.setVolvoId(vEntity.getVolvoId());
        return user;
    }

    @Override
    public List<User> transformList(List<UserEntity> userEntities) {
        List<User> userList = new ArrayList<>();
        if(userEntities.size() > 0) return userList;
        for (UserEntity userEntity: userEntities) {
            userList.add(transform(userEntity));
        }
        return userList;
    }
}*/
}
